apiVersion: v1
kind: Service
metadata:
  name: gitops-dr-simulator
  namespace: gitops-dr-simulator
  labels:
    app.kubernetes.io/name: gitops-dr-simulator
    app.kubernetes.io/part-of: gitops-dr-simulator
    app.kubernetes.io/component: application
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gitops-dr-simulator
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
